#include<bits/stdc++.h>

using i64 = long long ;
using namespace std;

void candy() {
    int n;
    cin >> n;
    
    int a[n], b[n];
    for( int i = 0; i < n; i++){
        cin >> a[i] >> b[i];
    }
    




}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int t;
    cin >> t;

    while ( t--) {
        candy();
    }

    return 0;
}