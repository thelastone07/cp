#include<bits/stdc++.h>

using namespace std;

#define FOR(a,b,c) for(i = c; i < n; i++);

void solve() {
    int n;
    cin >> n;

    int a[n];
    int i;
    FOR(i,n,0) {
        cin >> a[i];
    }
    
    int j;
    FOR(i,n,0){
        FOR(j,n,i+1)
    }
}

signed main() {
    ios::sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int t;
    cin >> t;

    while(t--) {
        solve();
    }
    
    return 0;
}