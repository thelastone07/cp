#include<bits/stdc++.h>

#define FOR(a, b) for(a = 0; a < b; a++);
#define val 1e9+7;
using namespace std;

void solve() {
    int n;
    cin >> n;

    int a[n], b[n];
    int i;
    FOR(i, n) {
        cin >> a[i];
    }
    FOR(i, n) {
        cin >> b[i];
    }
    
    
}

signed main() {
    ios :: sync_with_stdio(false);
    cin.tie(0); cout.tie(0);

    int t;
    cin >> t;

    while (t--){
        solve();
    }
}